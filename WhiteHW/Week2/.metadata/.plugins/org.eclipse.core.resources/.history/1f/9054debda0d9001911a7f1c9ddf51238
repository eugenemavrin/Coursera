#include <iostream>
#include <string>
using namespace std;

bool IsPalindrom(string s) {
	if (s == reverse(s.begin(), s.end())) return true;
	return false;
}

int main() {
	string s;
	cin >> s;
	cout << IsPalindrom(s);
	return 0;
}
