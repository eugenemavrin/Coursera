#include <iostream>
#include <set>
#include <string>
using namespace std;

int main() {
	set<set<string>> syn_voc;
	int Q;
	cin >> Q;
	while (Q > 0) {
		string queue;
		if (queue == "ADD") {
			string s1, s2;
			set<string> pair;
			cin >> s1 >> s2;
			pair = {s1, s2};
			syn_voc.insert(pair);

		}
		Q--;
	}
	return 0;
}
