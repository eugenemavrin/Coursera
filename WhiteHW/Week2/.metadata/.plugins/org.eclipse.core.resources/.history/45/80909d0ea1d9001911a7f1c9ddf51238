#include <iostream>
#include <string>
using namespace std;

bool IsPalindrom(string s) {
	return s == reverse(s.begin(), s.end());
}

int main() {
	string s;
	cin >> s;
	cout << IsPalindrom(s);
	return 0;
}
