#include <iostream>
#include <string>
using namespace std;

int main() {
	int n;
	cin >> n;
	string s;
	while (n > 1) {
		if (n % 2 == 1) {
			s += "1";
		}
		else {
			s+= "0";
		}
		n /= 2;
	}

	return 0;
}
