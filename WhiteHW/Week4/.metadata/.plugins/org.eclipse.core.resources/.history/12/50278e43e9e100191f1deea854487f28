#include <iostream>
#include <vector>
using namespace std;

struct Student {
	string first_name;
	string last_name;
	unsigned int day;
	unsigned int month;
	unsigned int year;
};

int main() {
	int n = 0;
	cin >> n;
	vector<int> s(n);
	//for (auto& s : students) {
		//cin >> s.first_name;
	//}

	return 0;
}
