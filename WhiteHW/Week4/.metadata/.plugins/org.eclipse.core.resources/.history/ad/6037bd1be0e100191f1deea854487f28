#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
	ifstream text("input.txt");
	string s;
	if(text.is_open()) {
		while (getline(text, s)) {
			cout << s << endl;
		}
	}
	else cout << "error!";
	return 0;
}
