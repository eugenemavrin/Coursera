#include <iostream>
using namespace std;

int main() {
	int * massiv = (int *) malloc(sizeof(int));
	cout << sizeof(massiv) << endl;
	int count = 1;
	int temp = 0;
	int i = 0;
	while(cin.peek() != '\n') {
		cin >> temp;
		massiv[i] = temp;
		i++;
		count++;
		massiv = (int*) realloc (massiv, 19 * sizeof(int));
	}
	cout << sizeof(massiv) << endl;
	/*
	for (int i = 0; i < n; ++i) {
		if(array[i] < 0) {
			int j = i;
			while(j < n-1) {
				array[j] = array[j + 1];
				j++;
			}
			counter++;
		}
	}
	*/
	for (int i = 0; i < 100; i++){
		cout << massiv[i] << " ";
	}
	free(massiv);

	return 0;
}
