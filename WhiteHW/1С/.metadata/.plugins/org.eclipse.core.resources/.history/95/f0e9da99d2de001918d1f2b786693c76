#include <iostream>
using namespace std;

int main() {
	string s;
	int *array = (int *) calloc(0, sizeof(int));
	int count = 1;
	int temp = 0;
	int i = 0;
	while(cin.peek() != '\n') {
		cin >> temp;
		array[i] = temp;
		i++;
		count++;
		array = (int*) realloc (array, 1100 * sizeof(int));
	}
	cout << array
	/*
	for (int i = 0; i < n; ++i) {
		if(array[i] < 0) {
			int j = i;
			while(j < n-1) {
				array[j] = array[j + 1];
				j++;
			}
			counter++;
		}
	}
	*/
	for (int i = 0; i < sizeof(array); i++){
		cout << array[i] << " ";
	}
	free(array);

	return 0;
}
